{
  "name": "xr-collab",
  "version": "1.1.0",
  "description": "XR Collab - WebXR协作平台",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "lodash.throttle": "^4.1.1"
  }
}
