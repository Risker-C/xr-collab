# 🔧 故障排查指南

## 问题：页面显示"未连接"

### 可能原因和解决方案

#### 1. Render后端冷启动
**现象**：首次访问或长时间未使用后显示未连接

**解决**：
- 等待15-30秒让Render后端唤醒
- 访问 https://xr-collab-backend.onrender.com/ 预热后端
- 刷新前端页面

#### 2. 浏览器控制台错误
**检查方法**：
- 按F12打开开发者工具
- 查看Console标签页
- 查找红色错误信息

**常见错误**：
- `CORS policy`: CORS配置问题（已修复）
- `WebSocket connection failed`: 降级到polling模式（已配置）
- `404 Not Found`: 后端未启动（等待Render部署）

#### 3. 测试后端状态
```bash
# 检查后端是否运行
curl https://xr-collab-backend.onrender.com/

# 应该返回：
# {"status":"ok","message":"XR Collab Backend Running","timestamp":"..."}

# 检查Socket.IO
curl "https://xr-collab-backend.onrender.com/socket.io/?EIO=4&transport=polling"

# 应该返回Socket.IO握手数据
```

#### 4. Render部署状态
1. 登录 https://dashboard.render.com
2. 找到 `xr-collab-backend` 服务
3. 查看 "Events" 标签
4. 确认最新部署状态为 "Live"

#### 5. Vercel部署状态
1. 登录 https://vercel.com/dashboard
2. 找到 `xr-collab` 项目
3. 查看最新部署状态
4. 确认状态为 "Ready"

---

## 问题：点击按钮没反应

### 解决方案

#### 1. 确保已连接
- 右上角状态显示"已连接"（绿色）
- 如果显示"未连接"，参考上面的连接问题

#### 2. 检查是否已加入房间
- 必须先输入用户名和房间ID
- 点击"进入房间"
- 控制面板才会出现

#### 3. 浏览器兼容性
**推荐浏览器**：
- ✅ Chrome / Edge（推荐）
- ✅ Firefox
- ✅ Safari
- ⚠️ IE不支持

#### 4. 清除缓存
```
Chrome: Ctrl+Shift+Delete
选择"缓存的图片和文件"
清除并刷新页面
```

---

## 问题：看不到其他用户

### 检查清单

- [ ] 两个用户使用相同的房间ID
- [ ] 两个用户都显示"已连接"
- [ ] 后端正常运行
- [ ] 刷新页面重试

---

## 问题：Worker计算不工作

**说明**：Workers是可选功能，不影响基本协作

**检查**：
- Workers可能需要15-30秒唤醒
- 查看右下角是否显示计算结果
- 浏览器控制台查看错误

---

## 问题：VR模式无法进入

**要求**：
- ✅ 必须使用HTTPS（生产环境已满足）
- ✅ VR设备已连接
- ✅ 浏览器支持WebXR

**测试**：
访问 https://immersive-web.github.io/webxr-samples/
如果能进入VR，说明设备和浏览器支持WebXR

---

## 当前部署状态（2026-02-20 21:43）

**后端**：https://xr-collab-backend.onrender.com
- 状态：⏳ 正在重新部署
- 预计完成：21:48

**前端**：https://xr-collab.vercel.app
- 状态：⏳ 正在重新部署
- 预计完成：21:45

**Workers**：
- Railway：✅ 运行中
- Koyeb：✅ 运行中

---

## 实时监控命令

```bash
# 持续监控后端
watch -n 5 'curl -s https://xr-collab-backend.onrender.com/ | jq'

# 检查Socket.IO
curl -s "https://xr-collab-backend.onrender.com/socket.io/?EIO=4&transport=polling"
```

---

## 获取帮助

如果以上方法都无法解决：
1. 检查GitHub Issues：https://github.com/Risker-C/xr-collab/issues
2. 查看完整文档：https://github.com/Risker-C/xr-collab
3. 联系开发者

---

**预计21:48后系统恢复正常** ⏰
